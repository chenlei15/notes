# Class 与 Style 绑定

##绑定 HTML Class
### 对象语法

- 如何给v-bind:class 传递一个对象，以动态地切换 class

### 数组语法

- 如何使用数组的语法给v-bind:class设置class

### 用在组件上
- 在组件上添加class时，组件的根元素的class是怎么样的

##绑定内联样式

- 如何使用对象语法绑定内联样式

- 如何使用数组语法绑定内联样式

# 条件渲染

##v-if

- 如何设置条件渲染元素，如何设置else块

- 如何v-if 条件渲染一整组元素

- Vue是如何复用已有元素

- 如何设置两个元素是完全独立的（不要复用它们）

## v-show
- v-show的效果和用法

## v-if vs v-show
- v-if和v-show各自的优劣

# 列表渲染
## v-for

- v-for的基本使用方法是什么

- 如何用v-for渲染多个元素块

- 如何用v-for进行对象迭代

- 如何用v-for进行整数迭代

- 如何在自定义组件里使用v-for

## key

- 在用v-for时指定key的作用

## 数组更新检测

- 有哪些观察数组的变异方法，

- 对于数组有哪些非变异方法

- 对于哪些变动的数组需要进行特殊处理

## 显示过滤/排序结果
- 在v-for时想要显示一个数组的过滤或排序副本，而不实际改变或重置原始数据

# 事件处理器

## 监听事件
- 如何使用指令监听 DOM 事件来触发一些 JavaScript 代码

- 如何使用指令监听 DOM 事件来触发一个定义的方法

## 事件修饰符

- 如何为v-on增加时间修饰符

## 键值修饰符

- 如何让v-on 监听键盘事件，

## 为什么在 HTML 中监听事件?
- 使用 v-on有哪些好处

# 表单控件绑定

## 基础用法

- 使用v-model时，表单元素的value、checked、selected 特性的初始值起什么作用

- 如何绑定文本到输入框

- 如何绑定多行文本到输入框

- 如何绑定到单个勾选框和多个勾选框

- 如何绑定单选按钮

- 如何绑定单选列表和多选列表

## 绑定 value

- 如何绑定表单 value 到 Vue 实例的一个动态属性上

## 修饰符
- v-model可以添加哪些修饰符，有什么作用










